# Docker compose to build and run the eureka service
version: '3'
services:
  hello-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports: 
      - "5010:5010"

networks:
  default:
    external:
      name: springboot